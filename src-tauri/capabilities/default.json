{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "默认能力：主窗口 + 自动更新",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "updater:default"
  ]
}
